---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Header from '@/components/Header.astro';
import Footer from '@/components/Footer.astro';
import { getTranslations } from '@/utils/i18n';
import type { Locale } from '@/i18n/config';

const locale: Locale = 'pt';
const t = await getTranslations(locale, 'common');

const seo = {
  title: 'Sobre a Sapiens IT',
  description: 'Sapiens IT é uma iniciativa independente criada por engenheiros de software experientes para elevar o padrão de arquitetura e engenharia de software em organizações modernas.',
  image: '/images/og-home.jpg'
};
---

<BaseLayout seo={seo} locale={locale} path="/about">
  <Header locale={locale} currentPath="/about" />
  
  <main class="min-h-screen pt-32">
    <article class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
      <header class="mb-12">
        <h1 class="text-5xl md:text-6xl font-bold text-gray-900 dark:text-white mb-6">
          Sobre a Sapiens IT
        </h1>
      </header>

      <div class="prose prose-lg dark:prose-invert max-w-none">
        <p class="text-xl text-gray-700 dark:text-gray-300 leading-relaxed mb-6">
          Sapiens IT é uma iniciativa criada por engenheiros de software seniores para compartilhar conhecimento técnico de alto nível e elevar o padrão de arquitetura e engenharia de software em organizações modernas.
        </p>

        <p class="text-xl text-gray-700 dark:text-gray-300 leading-relaxed mb-6">
          Nosso conteúdo é baseado em experiência prática projetando e mantendo sistemas complexos em indústrias como automotiva, finanças e manufatura. Cada artigo, cada arquitetura que discutimos, vem de problemas reais que enfrentamos em produção.
        </p>

        <p class="text-xl text-gray-700 dark:text-gray-300 leading-relaxed mb-6">
          Acreditamos que engenharia de excelência se constrói através de clareza, disciplina e foco no que realmente funciona. Não seguimos modas ou tendências sem questionar — validamos cada decisão técnica com base em resultados práticos e sustentabilidade a longo prazo.
        </p>

        <div class="mt-12 pt-8 border-t border-gray-200 dark:border-gray-700">
          <p class="text-lg text-gray-600 dark:text-gray-400">
            <a href="/manifesto" class="text-blue-600 dark:text-blue-400 hover:text-blue-700 dark:hover:text-blue-300 font-medium">
              Conheça nossos princípios →
            </a>
          </p>
        </div>
      </div>
    </article>
  </main>

  <Footer locale={locale} />
</BaseLayout>

